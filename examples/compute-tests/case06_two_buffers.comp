#version 450
layout(local_size_x = 64) in;

layout(std430, binding = 0) buffer InBuf {
  float in_data[];
} in_buf;

layout(std430, binding = 1) buffer OutBuf {
  float out_data[];
} out_buf;

void main() {
  uint i = gl_GlobalInvocationID.x;
  out_buf.out_data[i] = in_buf.in_data[i] * 3.0;
}
