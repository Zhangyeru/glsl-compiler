#version 450
layout(local_size_x = 64) in;

layout(std430, binding = 0) buffer Buf {
  float data[];
} buf;

void main() {
  uint i = gl_GlobalInvocationID.x;
  if ((i & 1u) == 0u) {
    buf.data[i] = buf.data[i] + 10.0;
  } else {
    buf.data[i] = buf.data[i] - 10.0;
  }
}
